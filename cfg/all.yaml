# Ultralytics YOLO 🚀, AGPL-3.0 license
# YOLO11 GhostHGNetV2 + DCNv4 + DynamicConv 融合模型

# Parameters
nc: 5 # number of classes
scales: # model compound scaling constants
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# YOLO11n backbone with 三重技术融合
backbone:
  # [from, repeats, module, args]
  - [-1, 1, HGStem, [32, 48]]  # 0-P2/4
  - [-1, 6, Ghost_HGBlock, [48, 128, 3]]  # stage 1 - 轻量化

  - [-1, 1, DWConv, [128, 3, 2, 1, False]]  # 2-P3/8
  - [-1, 6, Ghost_HGBlock, [96, 512, 3]]   # stage 2 - 轻量化

  - [-1, 1, DWConv, [512, 3, 2, 1, False]]  # 4-P4/16
  - [-1, 3, Ghost_HGBlock, [192, 1024, 5, True, False]]  # stage 3 前半
  - [-1, 3, C3k2_DCNv4, [1024, True]]  # stage 3 后半 - DCNv4增强感受野

  - [-1, 1, DWConv, [1024, 3, 2, 1, False]]  # 8-P5/32
  - [-1, 3, C3k2_DynamicConv, [2048, True]]  # stage 4 前半 - DynamicConv动态适应
  - [-1, 3, C3k2_DCNv4, [2048, True]]  # stage 4 后半 - DCNv4几何变换
  - [-1, 1, SPPF, [1024, 5]]  # 12
  - [-1, 2, C2PSA, [1024]] # 13

# YOLO11n head with 技术混合
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 9], 1, Concat, [1]] # cat backbone P4
  - [-1, 2, C3k2_DynamicConv, [512, False]] # 16

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]]
  - [[-1, 3], 1, Concat, [1]] # cat backbone P3
  - [-1, 2, C3k2, [256, False]] # 19 (P3/8-small) - 浅层保持标准卷积

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 16], 1, Concat, [1]] # cat head P4
  - [-1, 2, C3k2_DCNv4, [512, False]] # 22 (P4/16-medium)

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 13], 1, Concat, [1]] # cat head P5
  - [-1, 2, C3k2_DynamicConv, [1024, True]] # 25 (P5/32-large)

  - [[19, 22, 25], 1, Detect, [nc]] # Detect(P3, P4, P5)